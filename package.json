{
  "name": "jmlweb-tooling",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "format": "prettier --write \"**/*.{js,json,md}\"",
    "lint": "turbo run lint",
    "lint:root": "eslint .",
    "prepare": "husky",
    "test": "turbo run test",
    "validate": "pnpm format --check && pnpm lint:root && pnpm lint",
    "ci": "pnpm validate && pnpm build",
    "clean": "turbo run clean && rm -rf node_modules",
    "check": "turbo run check"
  },
  "lint-staged": {
    "*.{js,mjs,cjs}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@jmlweb/eslint-config-base-js": "workspace:*",
    "@jmlweb/prettier-config-base": "workspace:*",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-simple-import-sort": "^12.1.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.3.3",
    "tsup": "^8.5.1",
    "turbo": "^2.1.3",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@10.26.2"
}
